# Copy to apps/web/.env.local and packages/db/.env (or symlink one file)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ngb_dev?schema=public"
# Optional: public site URL for absolute metadata in production
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
